# sql script
"""CREATE TABLE lottery (
    lottery_id SERIAL PRIMARY KEY,
    prize VARCHAR(255),
    max_tickets BIGINT,
    ticket_price BIGINT,
    base_pot BIGINT,
    ends_on BIGINT,
    ended BOOLEAN,
    message_id BIGINT,
    thread_id BIGINT,
    image_link VARCHAR(512)
);"""
